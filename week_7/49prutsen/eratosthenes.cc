#include <iostream>
#include <thread>
#include <vector>
#include <chrono>

using namespace std;

void eratosthenes(vector<size_t> &vec, size_t nPrimes)
{
	cout << "eratosthenes called\n";
	//this_thread::sleep_for(chrono::seconds(1));
}
