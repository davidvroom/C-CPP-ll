#include "handler.ih"

void shift(ostream &out, string const &text);
{
	cout << text << '\n';
	for (size_t idx = 1; idx != text.size(); ++idx)
	{
		char first = text[0];
		text.replace(text.begin(), )
	}
}